<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>مبادرة الأستاذة غادة الحربي - لعبة من أنا؟</title>
    <style>
        :root {
            --primary-color: #2c3e50;
            --accent-color: #d4af37;
            --bg-color: #f4f7f6;
            --text-color: #34495e;
        }

        body {
            font-family: 'Segoe UI', Arial, sans-serif;
            background: linear-gradient(135deg, #e0eafc 0%, #cfdef3 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            padding: 10px; /* لضمان وجود مسافة صغيرة على الجوانب في الجوال */
            box-sizing: border-box;
        }

        .screen-board {
            background-color: white;
            width: 100%; /* جعل العرض كاملاً */
            max-width: 650px; /* لا يتجاوز هذا العرض على الكمبيوتر */
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0,0,0,0.2);
            border: 4px solid var(--primary-color);
            position: relative;
        }

        .header {
            background-color: var(--primary-color);
            color: white;
            padding: 15px;
            text-align: center;
            border-bottom: 5px solid var(--accent-color);
        }

        .header h2 {
            margin: 0;
            font-size: 1rem;
            font-weight: 300;
        }

        .header h1 {
            margin: 5px 0 0;
            font-size: 1.6rem; /* تصغير الخط قليلاً ليناسب الجوال */
            color: var(--accent-color);
        }

        .content {
            padding: 25px 15px; /* تقليل الحواف الداخلية */
            text-align: center;
        }

        .riddle-card {
            background: #fff;
            border-right: 6px solid var(--accent-color);
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            border-radius: 10px;
            min-height: 120px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem; /* حجم خط مريح للجوال */
            color: var(--text-color);
            line-height: 1.5;
        }

        .answer-area {
            display: none; /* إخفاء افتراضي */
            background: #f9f9f9;
            border-radius: 15px;
            padding: 15px;
            margin-bottom: 20px;
        }

        .answer-area.visible {
            display: block;
            animation: fadeIn 0.5s ease;
        }

        .result-row {
            display: flex;
            flex-direction: column; /* جعل النتائج فوق بعضها في الشاشات الصغيرة */
            gap: 10px;
            margin-top: 15px;
        }

        /* تعديل التصميم للشاشات الأكبر مثل الآيباد */
        @media (min-width: 480px) {
            .result-row { flex-direction: row; justify-content: space-around; }
            .header h1 { font-size: 2rem; }
            .riddle-card { font-size: 1.5rem; }
        }

        .badge {
            padding: 8px 15px;
            border-radius: 50px;
            font-weight: bold;
            display: inline-block;
        }

        .badge-word { background: #d4af37; color: white; margin-bottom: 10px; }
        .badge-syn { background: #27ae60; color: white; }
        .badge-ant { background: #e74c3c; color: white; }

        .btn-group {
            display: flex;
            flex-direction: column; /* أزرار تحت بعضها في الجوال لسهولة الضغط */
            gap: 10px;
        }

        @media (min-width: 480px) {
            .btn-group { flex-direction: row; justify-content: center; }
        }

        button {
            padding: 14px 25px;
            border: none;
            border-radius: 30px;
            cursor: pointer;
            font-size: 1.1rem;
            font-weight: bold;
            transition: 0.3s;
            width: 100%; /* زر عريض في الجوال */
        }

        @media (min-width: 480px) {
            button { width: auto; }
        }

        .reveal-btn { background-color: var(--primary-color); color: white; }
        .next-btn { background-color: var(--accent-color); color: var(--primary-color); }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        footer { margin-top: 15px; font-size: 0.8rem; color: #7f8c8d; }
    </style>
</head>
<body>

<div class="screen-board">
    <div class="header">
        <h2>مبادرة لغوية من تقديم</h2>
        <h1>الأستاذة غادة الحربي</h1>
    </div>

    <div class="content">
        <div class="riddle-card" id="riddleText">جاري تحميل اللغز...</div>

        <div class="answer-area" id="answerArea">
            <div class="badge badge-word" id="mainWord"></div>
            <div class="result-row">
                <div><span>المترادف: </span><span class="badge badge-syn" id="synonym"></span></div>
                <div><span>الضد: </span><span class="badge badge-ant" id="antonym"></span></div>
            </div>
        </div>

        <div class="btn-group">
            <button class="reveal-btn" onclick="reveal()">كشف الإجابة 🔍</button>
            <button class="next-btn" onclick="next()">اللغز التالي ✨</button>
        </div>

        <footer>لغة الضاد.. فخر واعتزاز</footer>
    </div>
</div>

<script>
    const data = [
        { q: "أنا بيت الأسد القوي، أختبئ بين الأشجار الكثيفة.. فمن أنا؟", a: "عرين", s: "مأوى / مسكن", ant: "خلاء" },
        { q: "أنا صفة للشخص الذي يساعد الجميع دون مقابل.. فمن أنا؟", a: "كريم", s: "سخي / معطاء", ant: "بخيل" },
        { q: "أنا الصعود إلى القمة، وعكس الهبوط.. فمن أنا؟", a: "علو", s: "ارتفاع / سمو", ant: "انخفاض" },
        { q: "أنا النور الذي يضيء الدرب بعد عتمة الليل.. فمن أنا؟", a: "الفجر", s: "الصباح / الضياء", ant: "الغسق" }
    ];

    let index = 0;

    function display() {
        document.getElementById('riddleText').innerText = data[index].q;
        document.getElementById('answerArea').classList.remove('visible');
    }

    function reveal() {
        document.getElementById('mainWord').innerText = "أنا: " + data[index].a;
        document.getElementById('synonym').innerText = data[index].s;
        document.getElementById('antonym').innerText = data[index].ant;
        document.getElementById('answerArea').classList.add('visible');
    }

    function next() {
        index = (index + 1) % data.length;
        display();
    }

    window.onload = display;
</script>

</body>
</html>